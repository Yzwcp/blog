<!DOCTYPE html>
<html lang="zh-CN">

<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="/lib/bootstrap-3.3.7-dist/css/bootstrap.min.css" rel="stylesheet">
    <title>blog</title>

</head>

<body>
    <div class="container">


        <div class="row">
            <div class=" col-md-3"></div>
            <div class=" col-md-6">
                <div class="panel panel-primary" style="margin-top: 200px;">
                    <div class="panel-heading">
                        <h3 class="panel-title text-center ">admin登录</h3>
                    </div>
                    <div class="panel-body">
                        <form id="loginForm" action="/admin/login" method ='post'>
                            <div class="input-group input-group-md" style="margin-bottom: 10px;">
                                <span  class="input-group-addon" id="sizing-addon1">邮件</span>
                                <input name="email" type="email" class="form-control" placeholder="Username"
                                    aria-describedby="sizing-addon1">
                            </div>
                            <div class="input-group input-group-md">
                                <span class="input-group-addon" id="sizing-addon1">密码</span>
                                <input type="password" name="password" class="form-control" placeholder="密码"
                                    aria-describedby="sizing-addon1">
                            </div>
                            <button class="btn btn-primary pull-right" style="margin-top: 5px;">登录</button>

                        </form>
                    </div>
                </div>
            </div>
        </div>
        <div class=" col-md-3"></div>
    </div>
    </div>

</body>

</html>
<!-- jQuery (Bootstrap 的所有 JavaScript 插件都依赖 jQuery，所以必须放在前边) -->
<script src="/lib/node_modules/jquery/dist/jquery.min.js"></script>
<!-- 加载 Bootstrap 的所有 JavaScript 插件。你也可以根据需要只加载单个插件。 -->
<script src="/lib/bootstrap-3.3.7-dist/js/bootstrap.min.js"></script>
<script src="/admin/js/common.js"></script>
<script>

    //为表单添加提交事件
    $('#loginForm').on('submit',function(){
        //获取表单中用户输入的内容
        //[{name:'email',value:'用户输入的内容'}]
        var form = serializeArray($(this))
        //添加表单判断
       
        if(form.email.trim().length == 0){
            alert('请输入邮箱')
        //如果用户没有输入邮件地址 阻止程序向下执行
            return false
        }
        if(form.password.trim().length == 0){
             alert('请输入密码')
        //如果用户没有输入邮件地址 阻止程序向下执行
            return false
        }
        //阻止表单默认提交行为
    })
    </script>
